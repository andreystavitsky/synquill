// GENERATED CODE - DO NOT MODIFY BY HAND
// Generated by synquill aggregate_builder

part of 'database.generated.dart';

/// Drift table definition for Contact entities
///
/// This table stores Contact data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@TableIndex(name: 'idx_contacts_userId', columns: {#userId})
@UseRowClass(Contact, constructor: 'fromDb')
class ContactTable extends Table {
  @override
  String get tableName => 'contacts';

  /// importedContactId column for Contact
  TextColumn get importedContactId => text().nullable()();

  /// userId column for Contact
  TextColumn get userId => text()();

  /// id column for Contact
  TextColumn get id => text().named('id')();

  /// birthday column for Contact
  DateTimeColumn get birthday => dateTime().nullable()();

  /// fullName column for Contact
  TextColumn get fullName => text().nullable()();

  /// avatarUrl column for Contact
  TextColumn get avatarUrl => text().nullable()();

  /// phoneNumber column for Contact
  TextColumn get phoneNumber => text().nullable()();

  /// email column for Contact
  TextColumn get email => text().nullable()();

  /// fetchedAt column for Contact
  DateTimeColumn get fetchedAt => dateTime()();

  /// lastSyncedAt column for Contact
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for Contact
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for Contact
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for Contact
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for Post entities
///
/// This table stores Post data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@TableIndex(name: 'idx_posts_userId', columns: {#userId})
@UseRowClass(Post, constructor: 'fromDb')
class PostTable extends Table {
  @override
  String get tableName => 'posts';

  /// id column for Post
  TextColumn get id => text().named('id')();

  /// title column for Post
  TextColumn get title => text()();

  /// body column for Post
  TextColumn get body => text()();

  /// userId column for Post
  TextColumn get userId => text()();

  /// lastSyncedAt column for Post
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for Post
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for Post
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for Post
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for LocalNote entities
///
/// This table stores LocalNote data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@TableIndex(name: 'idx_local_notes_ownerId', columns: {#ownerId})
@UseRowClass(LocalNote, constructor: 'fromDb')
class LocalNoteTable extends Table {
  @override
  String get tableName => 'local_notes';

  /// id column for LocalNote
  TextColumn get id => text().named('id')();

  /// ownerId column for LocalNote
  TextColumn get ownerId => text()();

  /// content column for LocalNote
  TextColumn get content => text()();

  /// category column for LocalNote
  TextColumn get category => text().nullable()();

  /// lastSyncedAt column for LocalNote
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for LocalNote
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for LocalNote
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for LocalNote
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for PlainModel entities
///
/// This table stores PlainModel data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@UseRowClass(PlainModel, constructor: 'fromDb')
class PlainModelTable extends Table {
  @override
  String get tableName => 'plain_models';

  /// id column for PlainModel
  TextColumn get id => text().named('id')();

  /// name column for PlainModel
  TextColumn get name => text()();

  /// value column for PlainModel
  IntColumn get value => integer()();

  /// lastSyncedAt column for PlainModel
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for PlainModel
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for PlainModel
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for PlainModel
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for User entities
///
/// This table stores User data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@UseRowClass(User, constructor: 'fromDb')
class UserTable extends Table {
  @override
  String get tableName => 'users';

  /// id column for User
  TextColumn get id => text().named('id')();

  /// name column for User
  TextColumn get name => text()();

  /// lastSyncedAt column for User
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for User
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for User
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for User
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for IndexingTestProduct entities
///
/// This table stores IndexingTestProduct data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@TableIndex(
    name: 'idx_indexing_test_products_categoryId', columns: {#categoryId})
@UseRowClass(IndexingTestProduct, constructor: 'fromDb')
class IndexingTestProductTable extends Table {
  @override
  String get tableName => 'indexing_test_products';

  /// id column for IndexingTestProduct
  TextColumn get id => text().named('id')();

  /// name column for IndexingTestProduct
  TextColumn get name => text()();

  /// categoryId column for IndexingTestProduct
  TextColumn get categoryId => text()();

  /// lastSyncedAt column for IndexingTestProduct
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for IndexingTestProduct
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for IndexingTestProduct
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for IndexingTestProduct
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for IndexingTestCategory entities
///
/// This table stores IndexingTestCategory data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@UseRowClass(IndexingTestCategory, constructor: 'fromDb')
class IndexingTestCategoryTable extends Table {
  @override
  String get tableName => 'indexing_test_categories';

  /// id column for IndexingTestCategory
  TextColumn get id => text().named('id')();

  /// name column for IndexingTestCategory
  TextColumn get name => text()();

  /// lastSyncedAt column for IndexingTestCategory
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for IndexingTestCategory
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for IndexingTestCategory
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for IndexingTestCategory
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for InvalidProduct entities
///
/// This table stores InvalidProduct data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@TableIndex(name: 'idx_invalid_products_categoryId', columns: {#categoryId})
@UseRowClass(InvalidProduct, constructor: 'fromDb')
class InvalidProductTable extends Table {
  @override
  String get tableName => 'invalid_products';

  /// id column for InvalidProduct
  TextColumn get id => text().named('id')();

  /// name column for InvalidProduct
  TextColumn get name => text()();

  /// categoryId column for InvalidProduct
  TextColumn get categoryId => text()();

  /// lastSyncedAt column for InvalidProduct
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for InvalidProduct
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for InvalidProduct
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for InvalidProduct
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for TestUser entities
///
/// This table stores TestUser data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@TableIndex(name: 'idx_test_users_email', columns: {#email}, unique: true)
@TableIndex(name: 'idx_test_users_username', columns: {#username})
@UseRowClass(TestUser, constructor: 'fromDb')
class TestUserTable extends Table {
  @override
  String get tableName => 'test_users';

  /// id column for TestUser
  TextColumn get id => text().named('id')();

  /// email column for TestUser
  TextColumn get email => text()();

  /// username column for TestUser
  TextColumn get username => text()();

  /// lastSyncedAt column for TestUser
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for TestUser
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for TestUser
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for TestUser
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for Category entities
///
/// This table stores Category data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@UseRowClass(Category, constructor: 'fromDb')
class CategoryTable extends Table {
  @override
  String get tableName => 'categories';

  /// id column for Category
  TextColumn get id => text().named('id')();

  /// name column for Category
  TextColumn get name => text()();

  /// color column for Category
  TextColumn get color => text()();

  /// lastSyncedAt column for Category
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for Category
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for Category
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for Category
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for ServerTestModel entities
///
/// This table stores ServerTestModel data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@UseRowClass(ServerTestModel, constructor: 'fromDb')
class ServerTestModelTable extends Table {
  @override
  String get tableName => 'server_test_models';

  /// id column for ServerTestModel
  TextColumn get id => text().named('id')();

  /// name column for ServerTestModel
  TextColumn get name => text()();

  /// description column for ServerTestModel
  TextColumn get description => text()();

  /// lastSyncedAt column for ServerTestModel
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for ServerTestModel
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for ServerTestModel
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for ServerTestModel
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for ClientTestModel entities
///
/// This table stores ClientTestModel data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@UseRowClass(ClientTestModel, constructor: 'fromDb')
class ClientTestModelTable extends Table {
  @override
  String get tableName => 'client_test_models';

  /// id column for ClientTestModel
  TextColumn get id => text().named('id')();

  /// name column for ClientTestModel
  TextColumn get name => text()();

  /// lastSyncedAt column for ClientTestModel
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for ClientTestModel
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for ClientTestModel
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for ClientTestModel
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for ServerParentModel entities
///
/// This table stores ServerParentModel data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@UseRowClass(ServerParentModel, constructor: 'fromDb')
class ServerParentModelTable extends Table {
  @override
  String get tableName => 'server_parent_models';

  /// id column for ServerParentModel
  TextColumn get id => text().named('id')();

  /// name column for ServerParentModel
  TextColumn get name => text()();

  /// category column for ServerParentModel
  TextColumn get category => text()();

  /// lastSyncedAt column for ServerParentModel
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for ServerParentModel
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for ServerParentModel
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for ServerParentModel
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for ServerChildModel entities
///
/// This table stores ServerChildModel data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@TableIndex(name: 'idx_server_child_models_parentId', columns: {#parentId})
@UseRowClass(ServerChildModel, constructor: 'fromDb')
class ServerChildModelTable extends Table {
  @override
  String get tableName => 'server_child_models';

  /// id column for ServerChildModel
  TextColumn get id => text().named('id')();

  /// name column for ServerChildModel
  TextColumn get name => text()();

  /// parentId column for ServerChildModel
  TextColumn get parentId => text()();

  /// data column for ServerChildModel
  TextColumn get data => text()();

  /// lastSyncedAt column for ServerChildModel
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for ServerChildModel
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for ServerChildModel
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for ServerChildModel
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for Department entities
///
/// This table stores Department data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@TableIndex(name: 'idx_departments_companyId', columns: {#companyId})
@UseRowClass(Department, constructor: 'fromDb')
class DepartmentTable extends Table {
  @override
  String get tableName => 'departments';

  /// id column for Department
  TextColumn get id => text().named('id')();

  /// name column for Department
  TextColumn get name => text()();

  /// companyId column for Department
  TextColumn get companyId => text()();

  /// lastSyncedAt column for Department
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for Department
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for Department
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for Department
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for Todo entities
///
/// This table stores Todo data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@TableIndex(name: 'idx_todos_userId', columns: {#userId})
@UseRowClass(Todo, constructor: 'fromDb')
class TodoTable extends Table {
  @override
  String get tableName => 'todos';

  /// title column for Todo
  TextColumn get title => text()();

  /// isCompleted column for Todo
  BoolColumn get isCompleted => boolean()();

  /// userId column for Todo
  TextColumn get userId => text()();

  /// id column for Todo
  TextColumn get id => text().named('id')();

  /// birthday column for Todo
  DateTimeColumn get birthday => dateTime().nullable()();

  /// fullName column for Todo
  TextColumn get fullName => text().nullable()();

  /// avatarUrl column for Todo
  TextColumn get avatarUrl => text().nullable()();

  /// phoneNumber column for Todo
  TextColumn get phoneNumber => text().nullable()();

  /// email column for Todo
  TextColumn get email => text().nullable()();

  /// fetchedAt column for Todo
  DateTimeColumn get fetchedAt => dateTime()();

  /// lastSyncedAt column for Todo
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for Todo
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for Todo
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for Todo
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for Project entities
///
/// This table stores Project data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@TableIndex(name: 'project_name', columns: {#name}, unique: true)
@TableIndex(name: 'idx_projects_ownerId', columns: {#ownerId})
@TableIndex(name: 'idx_projects_categoryId', columns: {#categoryId})
@UseRowClass(Project, constructor: 'fromDb')
class ProjectTable extends Table {
  @override
  String get tableName => 'projects';

  /// id column for Project
  TextColumn get id => text().named('id')();

  /// name column for Project
  TextColumn get name => text()();

  /// description column for Project
  TextColumn get description => text()();

  /// ownerId column for Project
  TextColumn get ownerId => text()();

  /// categoryId column for Project
  TextColumn get categoryId => text()();

  /// lastSyncedAt column for Project
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for Project
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for Project
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for Project
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}

/// Drift table definition for Company entities
///
/// This table stores Company data with automatic sync metadata
/// and supports offline storage with eventual consistency.
@TableIndex(name: 'idx_companies_departmentId', columns: {#departmentId})
@UseRowClass(Company, constructor: 'fromDb')
class CompanyTable extends Table {
  @override
  String get tableName => 'companies';

  /// id column for Company
  TextColumn get id => text().named('id')();

  /// name column for Company
  TextColumn get name => text()();

  /// departmentId column for Company
  TextColumn get departmentId => text().nullable()();

  /// lastSyncedAt column for Company
  DateTimeColumn get lastSyncedAt => dateTime().nullable()();

  /// createdAt column for Company
  DateTimeColumn get createdAt => dateTime().nullable()();

  /// updatedAt column for Company
  DateTimeColumn get updatedAt => dateTime().nullable()();

  /// syncStatus column for Company
  TextColumn get syncStatus => text()
      .nullable()
      .withDefault(const Constant('synced'))
      .map(const SyncStatusConverter())();

  @override
  Set<Column> get primaryKey => {id};
}
