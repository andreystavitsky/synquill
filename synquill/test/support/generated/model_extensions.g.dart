// GENERATED CODE - DO NOT MODIFY BY HAND
// Generated by synquill aggregate_builder

part of '../synquill.generated.dart';

/// Generated ID management extension for Contact
extension ContactIdManagement on Contact {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  Contact $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated extension methods for loading
/// related objects for Contact
extension ContactRelationExtensions on Contact {
  /// Logger for the Contact relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('ContactRelationExtensions');
    }
  }

  /// Load related User object
  /// Uses foreign key field 'userId'
  Future<User?> loadUser({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) async {
    try {
      final foreignKey = userId;
      if (foreignKey == null) return null;
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<User>(database);
      return await repository.findOne(
        foreignKey.toString(),
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to load User for $runtimeType[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }

  /// Watch related User object as a stream
  /// Uses foreign key field 'userId'
  /// Automatically switches to new target when foreign key changes
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<User?> watchUser({
    DataLoadPolicy? loadPolicy,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final sourceRepository =
          SynquillRepositoryProvider.getFrom<Contact>(database);
      final targetRepository =
          SynquillRepositoryProvider.getFrom<User>(database);

      // Watch the source object for changes in foreign key
      return sourceRepository
          .watchOne(
            id,
            loadPolicy: DataLoadPolicy.localOnly,
            watchRemote: watchRemote,
            retryOnFail: retryOnFail,
            headers: headers,
            extra: extra,
          )
          .map((sourceObject) {
            if (sourceObject == null) {
              return null;
            }

            final foreignKey = sourceObject.userId;
            if (foreignKey == null) {
              return null;
            }

            return sourceObject.userId.toString();
          })
          .distinct()
          .switchMap((foreignKey) {
            if (foreignKey == null) {
              return Stream.value(null);
            }

            // Switch to watching the target object
            return targetRepository.watchOne(
              foreignKey,
              loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
              watchRemote: watchRemote,
              retryOnFail: retryOnFail,
              headers: headers,
              extra: extra,
            );
          });
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to watch User for Contact[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }
}

/// Generated ID management extension for Post
extension PostIdManagement on Post {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  Post $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated extension methods for loading
/// related objects for Post
extension PostRelationExtensions on Post {
  /// Logger for the Post relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('PostRelationExtensions');
    }
  }

  /// Load related User object
  /// Uses foreign key field 'userId'
  Future<User?> loadUser({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) async {
    try {
      final foreignKey = userId;
      if (foreignKey == null) return null;
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<User>(database);
      return await repository.findOne(
        foreignKey.toString(),
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to load User for $runtimeType[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }

  /// Watch related User object as a stream
  /// Uses foreign key field 'userId'
  /// Automatically switches to new target when foreign key changes
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<User?> watchUser({
    DataLoadPolicy? loadPolicy,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final sourceRepository =
          SynquillRepositoryProvider.getFrom<Post>(database);
      final targetRepository =
          SynquillRepositoryProvider.getFrom<User>(database);

      // Watch the source object for changes in foreign key
      return sourceRepository
          .watchOne(
            id,
            loadPolicy: DataLoadPolicy.localOnly,
            watchRemote: watchRemote,
            retryOnFail: retryOnFail,
            headers: headers,
            extra: extra,
          )
          .map((sourceObject) {
            if (sourceObject == null) {
              return null;
            }

            final foreignKey = sourceObject.userId;
            if (foreignKey == null) {
              return null;
            }

            return sourceObject.userId.toString();
          })
          .distinct()
          .switchMap((foreignKey) {
            if (foreignKey == null) {
              return Stream.value(null);
            }

            // Switch to watching the target object
            return targetRepository.watchOne(
              foreignKey,
              loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
              watchRemote: watchRemote,
              retryOnFail: retryOnFail,
              headers: headers,
              extra: extra,
            );
          });
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to watch User for Post[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }
}

/// Generated ID management extension for LocalNote
extension LocalNoteIdManagement on LocalNote {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  LocalNote $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated extension methods for loading
/// related objects for LocalNote
extension LocalNoteRelationExtensions on LocalNote {
  /// Logger for the LocalNote relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('LocalNoteRelationExtensions');
    }
  }

  /// Load related User object
  /// Uses foreign key field 'ownerId'
  Future<User?> loadUser({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) async {
    try {
      final foreignKey = ownerId;
      if (foreignKey == null) return null;
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<User>(database);
      return await repository.findOne(
        foreignKey.toString(),
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to load User for $runtimeType[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }

  /// Watch related User object as a stream
  /// Uses foreign key field 'ownerId'
  /// Automatically switches to new target when foreign key changes
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<User?> watchUser({
    DataLoadPolicy? loadPolicy,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final sourceRepository =
          SynquillRepositoryProvider.getFrom<LocalNote>(database);
      final targetRepository =
          SynquillRepositoryProvider.getFrom<User>(database);

      // Watch the source object for changes in foreign key
      return sourceRepository
          .watchOne(
            id,
            loadPolicy: DataLoadPolicy.localOnly,
            watchRemote: watchRemote,
            retryOnFail: retryOnFail,
            headers: headers,
            extra: extra,
          )
          .map((sourceObject) {
            if (sourceObject == null) {
              return null;
            }

            final foreignKey = sourceObject.ownerId;
            if (foreignKey == null) {
              return null;
            }

            return sourceObject.ownerId.toString();
          })
          .distinct()
          .switchMap((foreignKey) {
            if (foreignKey == null) {
              return Stream.value(null);
            }

            // Switch to watching the target object
            return targetRepository.watchOne(
              foreignKey,
              loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
              watchRemote: watchRemote,
              retryOnFail: retryOnFail,
              headers: headers,
              extra: extra,
            );
          });
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to watch User for LocalNote[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }
}

/// Generated ID management extension for PlainModel
extension PlainModelIdManagement on PlainModel {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  PlainModel $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated ID management extension for User
extension UserIdManagement on User {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  User $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated extension methods for loading
/// related objects for User
extension UserRelationExtensions on User {
  /// Logger for the User relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('UserRelationExtensions');
    }
  }

  /// Load related Todos objects
  /// Uses mappedBy field 'userId' in Todo
  Future<List<Todo>> loadTodos({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
    QueryParams? queryParams,
  }) async {
    try {
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<Todo>(database);

      // Create required filter for the relation
      final requiredFilter = TodoFields.userId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return await repository.findAll(
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        queryParams: mergedQueryParams,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe('Failed to load Todos for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }

  /// Watch related Todos objects as a stream
  /// Uses mappedBy field 'userId' in Todo
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<List<Todo>> watchTodos({
    QueryParams? queryParams,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<Todo>(database);

      // Create required filter for the relation
      final requiredFilter = TodoFields.userId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return repository.watchAll(
        queryParams: mergedQueryParams,
        watchRemote: watchRemote,
        retryOnFail: retryOnFail,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe('Failed to watchTodos for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }

  /// Load related Posts objects
  /// Uses mappedBy field 'userId' in Post
  Future<List<Post>> loadPosts({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
    QueryParams? queryParams,
  }) async {
    try {
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<Post>(database);

      // Create required filter for the relation
      final requiredFilter = PostFields.userId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return await repository.findAll(
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        queryParams: mergedQueryParams,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe('Failed to load Posts for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }

  /// Watch related Posts objects as a stream
  /// Uses mappedBy field 'userId' in Post
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<List<Post>> watchPosts({
    QueryParams? queryParams,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<Post>(database);

      // Create required filter for the relation
      final requiredFilter = PostFields.userId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return repository.watchAll(
        queryParams: mergedQueryParams,
        watchRemote: watchRemote,
        retryOnFail: retryOnFail,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe('Failed to watchPosts for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }

  /// Load related Projects objects
  /// Uses mappedBy field 'ownerId' in Project
  Future<List<Project>> loadProjects({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
    QueryParams? queryParams,
  }) async {
    try {
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<Project>(database);

      // Create required filter for the relation
      final requiredFilter = ProjectFields.ownerId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return await repository.findAll(
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        queryParams: mergedQueryParams,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
          'Failed to load Projects for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }

  /// Watch related Projects objects as a stream
  /// Uses mappedBy field 'ownerId' in Project
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<List<Project>> watchProjects({
    QueryParams? queryParams,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<Project>(database);

      // Create required filter for the relation
      final requiredFilter = ProjectFields.ownerId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return repository.watchAll(
        queryParams: mergedQueryParams,
        watchRemote: watchRemote,
        retryOnFail: retryOnFail,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
          'Failed to watchProjects for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }

  /// Load related LocalNotes objects
  /// Uses mappedBy field 'ownerId' in LocalNote
  Future<List<LocalNote>> loadLocalNotes({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
    QueryParams? queryParams,
  }) async {
    try {
      final database = DatabaseProvider.instance;
      final repository =
          SynquillRepositoryProvider.getFrom<LocalNote>(database);

      // Create required filter for the relation
      final requiredFilter = LocalNoteFields.ownerId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return await repository.findAll(
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        queryParams: mergedQueryParams,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
          'Failed to load LocalNotes for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }

  /// Watch related LocalNotes objects as a stream
  /// Uses mappedBy field 'ownerId' in LocalNote
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<List<LocalNote>> watchLocalNotes({
    QueryParams? queryParams,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final repository =
          SynquillRepositoryProvider.getFrom<LocalNote>(database);

      // Create required filter for the relation
      final requiredFilter = LocalNoteFields.ownerId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return repository.watchAll(
        queryParams: mergedQueryParams,
        watchRemote: watchRemote,
        retryOnFail: retryOnFail,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
          'Failed to watchLocalNotes for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }
}

/// Generated ID management extension for IndexingTestProduct
extension IndexingTestProductIdManagement on IndexingTestProduct {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  IndexingTestProduct $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated extension methods for loading
/// related objects for IndexingTestProduct
extension IndexingTestProductRelationExtensions on IndexingTestProduct {
  /// Logger for the IndexingTestProduct relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('IndexingTestProductRelationExtensions');
    }
  }

  /// Load related IndexingTestCategory object
  /// Uses foreign key field 'categoryId'
  Future<IndexingTestCategory?> loadIndexingTestCategory({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) async {
    try {
      final foreignKey = categoryId;
      if (foreignKey == null) return null;
      final database = DatabaseProvider.instance;
      final repository =
          SynquillRepositoryProvider.getFrom<IndexingTestCategory>(database);
      return await repository.findOne(
        foreignKey.toString(),
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to load IndexingTestCategory for $runtimeType[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }

  /// Watch related IndexingTestCategory object as a stream
  /// Uses foreign key field 'categoryId'
  /// Automatically switches to new target when foreign key changes
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<IndexingTestCategory?> watchIndexingTestCategory({
    DataLoadPolicy? loadPolicy,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final sourceRepository =
          SynquillRepositoryProvider.getFrom<IndexingTestProduct>(database);
      final targetRepository =
          SynquillRepositoryProvider.getFrom<IndexingTestCategory>(database);

      // Watch the source object for changes in foreign key
      return sourceRepository
          .watchOne(
            id,
            loadPolicy: DataLoadPolicy.localOnly,
            watchRemote: watchRemote,
            retryOnFail: retryOnFail,
            headers: headers,
            extra: extra,
          )
          .map((sourceObject) {
            if (sourceObject == null) {
              return null;
            }

            final foreignKey = sourceObject.categoryId;
            if (foreignKey == null) {
              return null;
            }

            return sourceObject.categoryId.toString();
          })
          .distinct()
          .switchMap((foreignKey) {
            if (foreignKey == null) {
              return Stream.value(null);
            }

            // Switch to watching the target object
            return targetRepository.watchOne(
              foreignKey,
              loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
              watchRemote: watchRemote,
              retryOnFail: retryOnFail,
              headers: headers,
              extra: extra,
            );
          });
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to watch IndexingTestCategory for IndexingTestProduct[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }
}

/// Generated ID management extension for IndexingTestCategory
extension IndexingTestCategoryIdManagement on IndexingTestCategory {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  IndexingTestCategory $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated extension methods for loading
/// related objects for IndexingTestCategory
extension IndexingTestCategoryRelationExtensions on IndexingTestCategory {
  /// Logger for the IndexingTestCategory relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('IndexingTestCategoryRelationExtensions');
    }
  }

  /// Load related IndexingTestProducts objects
  /// Uses mappedBy field 'categoryId' in IndexingTestProduct
  Future<List<IndexingTestProduct>> loadIndexingTestProducts({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
    QueryParams? queryParams,
  }) async {
    try {
      final database = DatabaseProvider.instance;
      final repository =
          SynquillRepositoryProvider.getFrom<IndexingTestProduct>(database);

      // Create required filter for the relation
      final requiredFilter = IndexingTestProductFields.categoryId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return await repository.findAll(
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        queryParams: mergedQueryParams,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe('Failed to load IndexingTestProducts for $runtimeType[$id]',
          e, stackTrace);
      rethrow;
    }
  }

  /// Watch related IndexingTestProducts objects as a stream
  /// Uses mappedBy field 'categoryId' in IndexingTestProduct
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<List<IndexingTestProduct>> watchIndexingTestProducts({
    QueryParams? queryParams,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final repository =
          SynquillRepositoryProvider.getFrom<IndexingTestProduct>(database);

      // Create required filter for the relation
      final requiredFilter = IndexingTestProductFields.categoryId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return repository.watchAll(
        queryParams: mergedQueryParams,
        watchRemote: watchRemote,
        retryOnFail: retryOnFail,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe('Failed to watchIndexingTestProducts for $runtimeType[$id]',
          e, stackTrace);
      rethrow;
    }
  }
}

/// Generated ID management extension for InvalidProduct
extension InvalidProductIdManagement on InvalidProduct {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  InvalidProduct $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated extension methods for loading
/// related objects for InvalidProduct
extension InvalidProductRelationExtensions on InvalidProduct {
  /// Logger for the InvalidProduct relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('InvalidProductRelationExtensions');
    }
  }

  /// Load related IndexingTestCategory object
  /// Uses foreign key field 'categoryId'
  Future<IndexingTestCategory?> loadIndexingTestCategory({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) async {
    try {
      final foreignKey = categoryId;
      if (foreignKey == null) return null;
      final database = DatabaseProvider.instance;
      final repository =
          SynquillRepositoryProvider.getFrom<IndexingTestCategory>(database);
      return await repository.findOne(
        foreignKey.toString(),
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to load IndexingTestCategory for $runtimeType[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }

  /// Watch related IndexingTestCategory object as a stream
  /// Uses foreign key field 'categoryId'
  /// Automatically switches to new target when foreign key changes
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<IndexingTestCategory?> watchIndexingTestCategory({
    DataLoadPolicy? loadPolicy,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final sourceRepository =
          SynquillRepositoryProvider.getFrom<InvalidProduct>(database);
      final targetRepository =
          SynquillRepositoryProvider.getFrom<IndexingTestCategory>(database);

      // Watch the source object for changes in foreign key
      return sourceRepository
          .watchOne(
            id,
            loadPolicy: DataLoadPolicy.localOnly,
            watchRemote: watchRemote,
            retryOnFail: retryOnFail,
            headers: headers,
            extra: extra,
          )
          .map((sourceObject) {
            if (sourceObject == null) {
              return null;
            }

            final foreignKey = sourceObject.categoryId;
            if (foreignKey == null) {
              return null;
            }

            return sourceObject.categoryId.toString();
          })
          .distinct()
          .switchMap((foreignKey) {
            if (foreignKey == null) {
              return Stream.value(null);
            }

            // Switch to watching the target object
            return targetRepository.watchOne(
              foreignKey,
              loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
              watchRemote: watchRemote,
              retryOnFail: retryOnFail,
              headers: headers,
              extra: extra,
            );
          });
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to watch IndexingTestCategory for InvalidProduct[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }
}

/// Generated ID management extension for TestUser
extension TestUserIdManagement on TestUser {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  TestUser $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated ID management extension for Category
extension CategoryIdManagement on Category {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  Category $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated extension methods for loading
/// related objects for Category
extension CategoryRelationExtensions on Category {
  /// Logger for the Category relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('CategoryRelationExtensions');
    }
  }

  /// Load related Projects objects
  /// Uses mappedBy field 'categoryId' in Project
  Future<List<Project>> loadProjects({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
    QueryParams? queryParams,
  }) async {
    try {
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<Project>(database);

      // Create required filter for the relation
      final requiredFilter = ProjectFields.categoryId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return await repository.findAll(
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        queryParams: mergedQueryParams,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
          'Failed to load Projects for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }

  /// Watch related Projects objects as a stream
  /// Uses mappedBy field 'categoryId' in Project
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<List<Project>> watchProjects({
    QueryParams? queryParams,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<Project>(database);

      // Create required filter for the relation
      final requiredFilter = ProjectFields.categoryId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return repository.watchAll(
        queryParams: mergedQueryParams,
        watchRemote: watchRemote,
        retryOnFail: retryOnFail,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
          'Failed to watchProjects for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }
}

/// Generated ID management extension for ServerTestModel
extension ServerTestModelIdManagement on ServerTestModel {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => true;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  ServerTestModel $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// Note: Use repository.hasTemporaryId(model) for actual status
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// Note: Use repository.getTemporaryClientId(model) for actual value
  String? get $temporaryClientId => null;

  /// Note: Use repository.hasTemporaryId(model) to check temporary ID status
  /// Note: Use repository.getTemporaryClientId(model) to get temporary client ID
}

/// Generated ID management extension for ClientTestModel
extension ClientTestModelIdManagement on ClientTestModel {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  ClientTestModel $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated ID management extension for ServerParentModel
extension ServerParentModelIdManagement on ServerParentModel {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => true;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  ServerParentModel $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// Note: Use repository.hasTemporaryId(model) for actual status
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// Note: Use repository.getTemporaryClientId(model) for actual value
  String? get $temporaryClientId => null;

  /// Note: Use repository.hasTemporaryId(model) to check temporary ID status
  /// Note: Use repository.getTemporaryClientId(model) to get temporary client ID
}

/// Generated extension methods for loading
/// related objects for ServerParentModel
extension ServerParentModelRelationExtensions on ServerParentModel {
  /// Logger for the ServerParentModel relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('ServerParentModelRelationExtensions');
    }
  }

  /// Load related ServerChildModels objects
  /// Uses mappedBy field 'parentId' in ServerChildModel
  Future<List<ServerChildModel>> loadServerChildModels({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
    QueryParams? queryParams,
  }) async {
    try {
      final database = DatabaseProvider.instance;
      final repository =
          SynquillRepositoryProvider.getFrom<ServerChildModel>(database);

      // Create required filter for the relation
      final requiredFilter = ServerChildModelFields.parentId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return await repository.findAll(
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        queryParams: mergedQueryParams,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe('Failed to load ServerChildModels for $runtimeType[$id]', e,
          stackTrace);
      rethrow;
    }
  }

  /// Watch related ServerChildModels objects as a stream
  /// Uses mappedBy field 'parentId' in ServerChildModel
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<List<ServerChildModel>> watchServerChildModels({
    QueryParams? queryParams,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final repository =
          SynquillRepositoryProvider.getFrom<ServerChildModel>(database);

      // Create required filter for the relation
      final requiredFilter = ServerChildModelFields.parentId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return repository.watchAll(
        queryParams: mergedQueryParams,
        watchRemote: watchRemote,
        retryOnFail: retryOnFail,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe('Failed to watchServerChildModels for $runtimeType[$id]', e,
          stackTrace);
      rethrow;
    }
  }
}

/// Generated ID management extension for ServerChildModel
extension ServerChildModelIdManagement on ServerChildModel {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => true;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  ServerChildModel $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// Note: Use repository.hasTemporaryId(model) for actual status
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// Note: Use repository.getTemporaryClientId(model) for actual value
  String? get $temporaryClientId => null;

  /// Note: Use repository.hasTemporaryId(model) to check temporary ID status
  /// Note: Use repository.getTemporaryClientId(model) to get temporary client ID
}

/// Generated extension methods for loading
/// related objects for ServerChildModel
extension ServerChildModelRelationExtensions on ServerChildModel {
  /// Logger for the ServerChildModel relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('ServerChildModelRelationExtensions');
    }
  }

  /// Load related ServerParentModel object
  /// Uses foreign key field 'parentId'
  Future<ServerParentModel?> loadServerParentModel({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) async {
    try {
      final foreignKey = parentId;
      if (foreignKey == null) return null;
      final database = DatabaseProvider.instance;
      final repository =
          SynquillRepositoryProvider.getFrom<ServerParentModel>(database);
      return await repository.findOne(
        foreignKey.toString(),
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to load ServerParentModel for $runtimeType[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }

  /// Watch related ServerParentModel object as a stream
  /// Uses foreign key field 'parentId'
  /// Automatically switches to new target when foreign key changes
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<ServerParentModel?> watchServerParentModel({
    DataLoadPolicy? loadPolicy,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final sourceRepository =
          SynquillRepositoryProvider.getFrom<ServerChildModel>(database);
      final targetRepository =
          SynquillRepositoryProvider.getFrom<ServerParentModel>(database);

      // Watch the source object for changes in foreign key
      return sourceRepository
          .watchOne(
            id,
            loadPolicy: DataLoadPolicy.localOnly,
            watchRemote: watchRemote,
            retryOnFail: retryOnFail,
            headers: headers,
            extra: extra,
          )
          .map((sourceObject) {
            if (sourceObject == null) {
              return null;
            }

            final foreignKey = sourceObject.parentId;
            if (foreignKey == null) {
              return null;
            }

            return sourceObject.parentId.toString();
          })
          .distinct()
          .switchMap((foreignKey) {
            if (foreignKey == null) {
              return Stream.value(null);
            }

            // Switch to watching the target object
            return targetRepository.watchOne(
              foreignKey,
              loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
              watchRemote: watchRemote,
              retryOnFail: retryOnFail,
              headers: headers,
              extra: extra,
            );
          });
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to watch ServerParentModel for ServerChildModel[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }
}

/// Generated ID management extension for Department
extension DepartmentIdManagement on Department {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  Department $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated extension methods for loading
/// related objects for Department
extension DepartmentRelationExtensions on Department {
  /// Logger for the Department relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('DepartmentRelationExtensions');
    }
  }

  /// Load related Company object
  /// Uses foreign key field 'companyId'
  Future<Company?> loadCompany({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) async {
    try {
      final foreignKey = companyId;
      if (foreignKey == null) return null;
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<Company>(database);
      return await repository.findOne(
        foreignKey.toString(),
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to load Company for $runtimeType[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }

  /// Watch related Company object as a stream
  /// Uses foreign key field 'companyId'
  /// Automatically switches to new target when foreign key changes
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<Company?> watchCompany({
    DataLoadPolicy? loadPolicy,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final sourceRepository =
          SynquillRepositoryProvider.getFrom<Department>(database);
      final targetRepository =
          SynquillRepositoryProvider.getFrom<Company>(database);

      // Watch the source object for changes in foreign key
      return sourceRepository
          .watchOne(
            id,
            loadPolicy: DataLoadPolicy.localOnly,
            watchRemote: watchRemote,
            retryOnFail: retryOnFail,
            headers: headers,
            extra: extra,
          )
          .map((sourceObject) {
            if (sourceObject == null) {
              return null;
            }

            final foreignKey = sourceObject.companyId;
            if (foreignKey == null) {
              return null;
            }

            return sourceObject.companyId.toString();
          })
          .distinct()
          .switchMap((foreignKey) {
            if (foreignKey == null) {
              return Stream.value(null);
            }

            // Switch to watching the target object
            return targetRepository.watchOne(
              foreignKey,
              loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
              watchRemote: watchRemote,
              retryOnFail: retryOnFail,
              headers: headers,
              extra: extra,
            );
          });
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to watch Company for Department[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }

  /// Load related Companies objects
  /// Uses mappedBy field 'departmentId' in Company
  Future<List<Company>> loadCompanies({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
    QueryParams? queryParams,
  }) async {
    try {
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<Company>(database);

      // Create required filter for the relation
      final requiredFilter = CompanyFields.departmentId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return await repository.findAll(
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        queryParams: mergedQueryParams,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
          'Failed to load Companies for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }

  /// Watch related Companies objects as a stream
  /// Uses mappedBy field 'departmentId' in Company
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<List<Company>> watchCompanies({
    QueryParams? queryParams,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<Company>(database);

      // Create required filter for the relation
      final requiredFilter = CompanyFields.departmentId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return repository.watchAll(
        queryParams: mergedQueryParams,
        watchRemote: watchRemote,
        retryOnFail: retryOnFail,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
          'Failed to watchCompanies for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }
}

/// Generated ID management extension for Todo
extension TodoIdManagement on Todo {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  Todo $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated extension methods for loading
/// related objects for Todo
extension TodoRelationExtensions on Todo {
  /// Logger for the Todo relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('TodoRelationExtensions');
    }
  }

  /// Load related User object
  /// Uses foreign key field 'userId'
  Future<User?> loadUser({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) async {
    try {
      final foreignKey = userId;
      if (foreignKey == null) return null;
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<User>(database);
      return await repository.findOne(
        foreignKey.toString(),
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to load User for $runtimeType[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }

  /// Watch related User object as a stream
  /// Uses foreign key field 'userId'
  /// Automatically switches to new target when foreign key changes
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<User?> watchUser({
    DataLoadPolicy? loadPolicy,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final sourceRepository =
          SynquillRepositoryProvider.getFrom<Todo>(database);
      final targetRepository =
          SynquillRepositoryProvider.getFrom<User>(database);

      // Watch the source object for changes in foreign key
      return sourceRepository
          .watchOne(
            id,
            loadPolicy: DataLoadPolicy.localOnly,
            watchRemote: watchRemote,
            retryOnFail: retryOnFail,
            headers: headers,
            extra: extra,
          )
          .map((sourceObject) {
            if (sourceObject == null) {
              return null;
            }

            final foreignKey = sourceObject.userId;
            if (foreignKey == null) {
              return null;
            }

            return sourceObject.userId.toString();
          })
          .distinct()
          .switchMap((foreignKey) {
            if (foreignKey == null) {
              return Stream.value(null);
            }

            // Switch to watching the target object
            return targetRepository.watchOne(
              foreignKey,
              loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
              watchRemote: watchRemote,
              retryOnFail: retryOnFail,
              headers: headers,
              extra: extra,
            );
          });
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to watch User for Todo[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }
}

/// Generated ID management extension for Project
extension ProjectIdManagement on Project {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  Project $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated extension methods for loading
/// related objects for Project
extension ProjectRelationExtensions on Project {
  /// Logger for the Project relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('ProjectRelationExtensions');
    }
  }

  /// Load related User object
  /// Uses foreign key field 'ownerId'
  Future<User?> loadUser({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) async {
    try {
      final foreignKey = ownerId;
      if (foreignKey == null) return null;
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<User>(database);
      return await repository.findOne(
        foreignKey.toString(),
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to load User for $runtimeType[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }

  /// Watch related User object as a stream
  /// Uses foreign key field 'ownerId'
  /// Automatically switches to new target when foreign key changes
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<User?> watchUser({
    DataLoadPolicy? loadPolicy,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final sourceRepository =
          SynquillRepositoryProvider.getFrom<Project>(database);
      final targetRepository =
          SynquillRepositoryProvider.getFrom<User>(database);

      // Watch the source object for changes in foreign key
      return sourceRepository
          .watchOne(
            id,
            loadPolicy: DataLoadPolicy.localOnly,
            watchRemote: watchRemote,
            retryOnFail: retryOnFail,
            headers: headers,
            extra: extra,
          )
          .map((sourceObject) {
            if (sourceObject == null) {
              return null;
            }

            final foreignKey = sourceObject.ownerId;
            if (foreignKey == null) {
              return null;
            }

            return sourceObject.ownerId.toString();
          })
          .distinct()
          .switchMap((foreignKey) {
            if (foreignKey == null) {
              return Stream.value(null);
            }

            // Switch to watching the target object
            return targetRepository.watchOne(
              foreignKey,
              loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
              watchRemote: watchRemote,
              retryOnFail: retryOnFail,
              headers: headers,
              extra: extra,
            );
          });
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to watch User for Project[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }

  /// Load related Category object
  /// Uses foreign key field 'categoryId'
  Future<Category?> loadCategory({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) async {
    try {
      final foreignKey = categoryId;
      if (foreignKey == null) return null;
      final database = DatabaseProvider.instance;
      final repository = SynquillRepositoryProvider.getFrom<Category>(database);
      return await repository.findOne(
        foreignKey.toString(),
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to load Category for $runtimeType[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }

  /// Watch related Category object as a stream
  /// Uses foreign key field 'categoryId'
  /// Automatically switches to new target when foreign key changes
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<Category?> watchCategory({
    DataLoadPolicy? loadPolicy,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final sourceRepository =
          SynquillRepositoryProvider.getFrom<Project>(database);
      final targetRepository =
          SynquillRepositoryProvider.getFrom<Category>(database);

      // Watch the source object for changes in foreign key
      return sourceRepository
          .watchOne(
            id,
            loadPolicy: DataLoadPolicy.localOnly,
            watchRemote: watchRemote,
            retryOnFail: retryOnFail,
            headers: headers,
            extra: extra,
          )
          .map((sourceObject) {
            if (sourceObject == null) {
              return null;
            }

            final foreignKey = sourceObject.categoryId;
            if (foreignKey == null) {
              return null;
            }

            return sourceObject.categoryId.toString();
          })
          .distinct()
          .switchMap((foreignKey) {
            if (foreignKey == null) {
              return Stream.value(null);
            }

            // Switch to watching the target object
            return targetRepository.watchOne(
              foreignKey,
              loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
              watchRemote: watchRemote,
              retryOnFail: retryOnFail,
              headers: headers,
              extra: extra,
            );
          });
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to watch Category for Project[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }
}

/// Generated ID management extension for Company
extension CompanyIdManagement on Company {
  /// Whether this model uses server-generated IDs
  bool get $usesServerGeneratedId => false;

  /// Create a new model instance with a different ID
  /// This is used during ID negotiation when server assigns a different ID
  Company $replaceIdEverywhere(String newId) {
    final json = toJson();
    json['id'] = newId;
    return fromJson(json);
  }

  /// Whether this model currently has a temporary ID
  /// (always false for client-generated IDs)
  bool get $hasTemporaryId => false;

  /// Get temporary client ID for this model
  /// (always null for client-generated IDs)
  String? get $temporaryClientId => null;
}

/// Generated extension methods for loading
/// related objects for Company
extension CompanyRelationExtensions on Company {
  /// Logger for the Company relation extensions.
  static Logger get _log {
    try {
      return SynquillStorage.logger;
    } catch (_) {
      return Logger('CompanyRelationExtensions');
    }
  }

  /// Load related Departments objects
  /// Uses mappedBy field 'companyId' in Department
  Future<List<Department>> loadDepartments({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
    QueryParams? queryParams,
  }) async {
    try {
      final database = DatabaseProvider.instance;
      final repository =
          SynquillRepositoryProvider.getFrom<Department>(database);

      // Create required filter for the relation
      final requiredFilter = DepartmentFields.companyId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return await repository.findAll(
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        queryParams: mergedQueryParams,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
          'Failed to load Departments for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }

  /// Watch related Departments objects as a stream
  /// Uses mappedBy field 'companyId' in Department
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<List<Department>> watchDepartments({
    QueryParams? queryParams,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final repository =
          SynquillRepositoryProvider.getFrom<Department>(database);

      // Create required filter for the relation
      final requiredFilter = DepartmentFields.companyId.equals(id);

      // Merge user queryParams with required filter
      final mergedQueryParams = QueryParams.withRequiredFilter(
        queryParams,
        requiredFilter,
      );

      return repository.watchAll(
        queryParams: mergedQueryParams,
        watchRemote: watchRemote,
        retryOnFail: retryOnFail,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
          'Failed to watchDepartments for $runtimeType[$id]', e, stackTrace);
      rethrow;
    }
  }

  /// Load related Department object
  /// Uses foreign key field 'departmentId'
  Future<Department?> loadDepartment({
    DataLoadPolicy? loadPolicy,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) async {
    try {
      final foreignKey = departmentId;
      if (foreignKey == null) return null;
      final database = DatabaseProvider.instance;
      final repository =
          SynquillRepositoryProvider.getFrom<Department>(database);
      return await repository.findOne(
        foreignKey.toString(),
        loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
        headers: headers,
        extra: extra,
      );
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to load Department for $runtimeType[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }

  /// Watch related Department object as a stream
  /// Uses foreign key field 'departmentId'
  /// Automatically switches to new target when foreign key changes
  ///
  /// WARNING: Enabling [watchRemote] on relationships can lead to a high volume
  /// of concurrent WebSocket subscriptions if used inside list item views (N+1 subscriptions).
  Stream<Department?> watchDepartment({
    DataLoadPolicy? loadPolicy,
    bool watchRemote = false,
    bool retryOnFail = true,
    Map<String, String>? headers,
    Map<String, dynamic>? extra,
  }) {
    try {
      final database = DatabaseProvider.instance;
      final sourceRepository =
          SynquillRepositoryProvider.getFrom<Company>(database);
      final targetRepository =
          SynquillRepositoryProvider.getFrom<Department>(database);

      // Watch the source object for changes in foreign key
      return sourceRepository
          .watchOne(
            id,
            loadPolicy: DataLoadPolicy.localOnly,
            watchRemote: watchRemote,
            retryOnFail: retryOnFail,
            headers: headers,
            extra: extra,
          )
          .map((sourceObject) {
            if (sourceObject == null) {
              return null;
            }

            final foreignKey = sourceObject.departmentId;
            if (foreignKey == null) {
              return null;
            }

            return sourceObject.departmentId.toString();
          })
          .distinct()
          .switchMap((foreignKey) {
            if (foreignKey == null) {
              return Stream.value(null);
            }

            // Switch to watching the target object
            return targetRepository.watchOne(
              foreignKey,
              loadPolicy: loadPolicy ?? DataLoadPolicy.localOnly,
              watchRemote: watchRemote,
              retryOnFail: retryOnFail,
              headers: headers,
              extra: extra,
            );
          });
    } catch (e, stackTrace) {
      _log.severe(
        'Failed to watch Department for Company[$id]',
        e,
        stackTrace,
      );
      rethrow;
    }
  }
}
