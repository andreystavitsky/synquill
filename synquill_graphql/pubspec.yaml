name: synquill_graphql
description: GraphQL adapter for the Synquill offline-first data engine.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  dio: ^5.8.0+1
  logging: ^1.3.0
  meta: ^1.3.0
  synquill:
    path: ../synquill

dev_dependencies:
  build_runner: ^2.4.14
  lints: ^6.0.0
  mockito: any
  test: any
